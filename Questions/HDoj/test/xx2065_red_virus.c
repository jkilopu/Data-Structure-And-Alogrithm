/* 一开始想分类，但是方向错误
 * 后来看来题解，真是牛啊
 * 不过不想重新打一遍代码了
 */
#include <stdio.h>
#include <limits.h>
int main(void)
{
    int N, res;
    long n;

    while(scanf("%d", &N) && N != 0)
    {
        while(N--)
        {
            scanf("%ld", &n);
        }
    }
}